{% extends 'layouts/admin/admin.html.twig' %}

{% block title %}Admin - {{ parametres.structure ? parametres.structure.name : company_name }} - {{ title }}{% endblock %}

{% block body %}
    <div class="container-fluid flex-grow-1 container-p-y">
        <h4 class="font-weight-bold py-3 mb-0">Editer un produit</h4>
        <div class="text-muted small mt-0 mb-4 d-block breadcrumb">
            {{ fil_arial_admin_categories(title, 'Shop', 'Produits') }}
        </div>

        <div class="row">
            <div class="col-md-12">
                {{ include('admin/shops/products/_form.html.twig') }}
            </div>
        </div>
    </div>

{% endblock %}
{% block javascripts %}
    <script>
        $('.switcher-input').unwrap();
    </script>
{% endblock %}

