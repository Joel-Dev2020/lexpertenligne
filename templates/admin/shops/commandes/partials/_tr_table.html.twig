<tr>
    <td>{{ commande.reference }}</td>
    <td class="text-50 js-lists-values-activity small">
        {{ commande.createdAt|date('d/m/Y à H:i:s') }}
    </td>
    <td class="text-50 js-lists-values-activity small">{{ commande.totalttc|devise }}</td>
    <td class="text-50 js-lists-values-activity small">
        <a href="javascript:void(0)" data-toggle="popover" data-placement="right" data-content="{{ commande.motifs|nl2br }}" title="Motif du status" class="badge badge-dark" style="background-color: {{ commande.status.color }}!important;">{{ commande.status.name }}</a>
    </td>
    <td class="text-right pl-0">
        <a href="{{ path('admin.commandes.invoice', {'id': commande.id}) }}" class="text-50"><i class="material-icons">folder</i></a>
        <a href="{{ path('admin.commandes.edit', {'id': commande.id}) }}" class="text-50"><i class="material-icons">edit</i></a>
        {% include 'admin/shops/commandes/_delete_form.html.twig' %}
    </td>
</tr>