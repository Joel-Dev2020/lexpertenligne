<form method="post" action="{{ path('admin.logs.delete', {'id': log.id}) }}" onsubmit="return confirm('Etes vous sûr de supprimer ce log?');" style="display: inline !important;">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ log.id) }}">
    <button class="btn btn-xs btn-danger float-right"><i class="fa fa-trash"></i></button>
</form>
