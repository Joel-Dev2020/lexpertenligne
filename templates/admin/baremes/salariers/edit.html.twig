{% extends 'layouts/admin/admin.html.twig' %}

{% block title %}Admin - {{ parametres.structure ? parametres.structure.name : company_name }} - {{ title }}{% endblock %}

{% block body %}
    <div class="pt-32pt">
        <div class="container-fluid page__container d-flex flex-column flex-md-row align-items-center text-center text-sm-left">
            {{ fil_arial_admin_categories(title, 'Salariers', '') }}
            <div class="row" role="tablist">
                <div class="col-auto">
                    <a href="{{ path('admin.salariers.index') }}" class="btn btn-outline-secondary">Liste des types salariers</a> &nbsp;&nbsp;
                    <a href="{{ path('admin.salariers.new') }}" class="btn btn-outline-secondary">Nouveau type salarier</a>
                </div>
            </div>
        </div>
    </div>

    <div class="page-section container-fluid page__container">
        <div class="page-separator">
            <div class="page-separator__text">Editer un salarier</div>
        </div>
        <div class="col-md-6 p-0">
            <div class="card p-relativemb-0">
                <div class="card-body">
                    {{ include('admin/baremes/salariers/_form.html.twig', {'button_label': 'Sauvegarder les informations'}) }}
                </div>
            </div>
        </div>
    </div>

{% endblock %}