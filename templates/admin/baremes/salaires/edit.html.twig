{% extends 'layouts/admin/admin.html.twig' %}

{% block title %}Admin - {{ parametres.structure ? parametres.structure.name : company_name }} - {{ title }}{% endblock %}

{% block body %}
    <div class="pt-32pt">
        <div class="container-fluid page__container d-flex flex-column flex-md-row align-items-center text-center text-sm-left">
            {{ fil_arial_admin_categories(title, 'Salaires', '') }}
            <div class="row" role="tablist">
                <div class="col-auto">
                    <a href="{{ path('admin.salaires.index') }}" class="btn btn-outline-secondary">Liste des salaires</a> &nbsp;&nbsp;
                    <a href="{{ path('admin.salaires.new') }}" class="btn btn-outline-secondary">Nouveau salaire</a>
                </div>
            </div>
        </div>
    </div>

    <div class="page-section container-fluid page__container">
        <div class="page-separator">
            <div class="page-separator__text">Editer un salaire</div>
        </div>
        <div class="col-md-6 p-0">
            <div class="card p-relativemb-0">
                <div class="card-body">
                    {{ include('admin/baremes/salaires/_form.html.twig', {'button_label': 'Sauvegarder les informations'}) }}
                </div>
            </div>
        </div>
    </div>

{% endblock %}