{% if product.filename %}
    <img src="{{ vich_uploader_asset(product, 'imageFile') | imagine_filter('thumbnail') }}" alt="{{ product.name }}" class="card-img">
{% else %}
    <img src="{{ asset('assetics/images/empty/small.png') | imagine_filter('thumbnail') }}" alt="{{ product.name }}" class="card-img">
{% endif %}