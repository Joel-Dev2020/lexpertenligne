{% if tags|length > 0 %}
    <h2>Mots clés</h2>
    <div class="tag_widget">
        <ul>
            {% for index,tag in tags %}
                <li><a href="{{ path('shop.products.tags', {'id': tag.id, 'slug': tag.slug}) }}">{{ tag.name }}</a></li>
            {% endfor %}
        </ul>
    </div>
{% endif %}