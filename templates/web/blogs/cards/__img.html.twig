{% if blog.filename %}
    <img src="{{ vich_uploader_asset(blog, 'imageFile') | imagine_filter('thumbnail') }}" alt="{{ blog.name }}" class="card-img">
{% else %}
    <img src="{{ asset('assetics/images/empty/small.png') | imagine_filter('thumbnail') }}" alt="{{ blog.name }}" class="card-img">
{% endif %}