<div class="page-separator">
    <div class="page-separator__text">Dossiers récents</div>
</div>
<p class="text-70 mb-24pt">People who started the most discussions on Luma.</p>

<div class="mb-4">
    {% for dossier in dossiers %}
        <div class="d-flex align-items-center mb-2">
            <a href="{{ path('dossiers.show', {'id': dossier.id, 'slug': dossier.slug}) }}" class="avatar avatar-xs mr-8pt">
                {% include 'web/dossiers/cards/__img_small.html.twig' %}
            </a>
            <a href="{{ path('dossiers.show', {'id': dossier.id, 'slug': dossier.slug}) }}" class="flex mr-2 text-body">
                <strong>{{ dossier.name|u.truncate(30, '...') }}</strong>
            </a>
            <span class="text-70 mr-2">{{ dossier.commentaire.count }}</span>
            <i class="text-muted material-icons font-size-16pt">forum</i>
        </div>
    {% endfor %}

</div>