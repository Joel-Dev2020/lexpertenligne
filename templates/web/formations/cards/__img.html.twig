{% if formation.filename %}
    <img src="{{ vich_uploader_asset(formation, 'imageFile') | imagine_filter('thumbnail') }}" alt="{{ formation.name }}" class="card-img">
{% else %}
    <img src="{{ asset('assetics/images/empty/small.png') | imagine_filter('thumbnail') }}" alt="{{ formation.name }}" class="card-img">
{% endif %}